export type SecureNativeOptions = {
  apiKey?: string;
  appName?: string;
  hostId?: string;
  apiUrl?: string;
  interval?: number;
  heartBeatInterval?: number;
  maxEvents?: number;
  timeout?: number;
  autoSend?: boolean;
  disable?: boolean;
  debugMode?: boolean;
  minSupportedVersion?: string;
}
